name: Render and Deploy Quarto Site

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      # Installer Quarto
      - uses: quarto-dev/quarto-actions/setup@v2
        with:
          version: "1.4.554"   # adapte si besoin

      # Installer R
      - uses: r-lib/actions/setup-r@v2
        with:
          r-version: "4.3.2"   # adapte si besoin

      # Installer dépendances système (utile si tu gardes sf, arrow, etc.)
      - name: Install system dependencies
        run: |
          sudo apt-get update -y
          sudo apt-get install -y \
            make \
            cmake \
            gdal-bin \
            libgdal-dev \
            libproj-dev \
            libudunits2-dev \
            libgeos-dev \
            libpng-dev \
            pandoc

      # Mettre en cache les packages R
      - name: Cache R packages
        uses: actions/cache@v3
        with:
          path: ${{ env.R_LIBS_USER }}
          key: ${{ runner.os }}-r-${{ hashFiles('**/renv.lock') }}
          restore-keys: ${{ runner.os }}-r-

      # Restaurer avec renv (sans terra !)
      - name: Restore R packages with renv
        run: |
          Rscript -e 'install.packages("renv", repos = "https://cloud.r-project.org")'
          Rscript -e 'renv::restore(prompt = FALSE)'

      # Rendre le projet Quarto
      - name: Render Quarto Project
        run: quarto render

      # Déployer sur GitHub Pages
      - uses: actions/upload-pages-artifact@v2
        with:
          path: _site

  deploy:
    runs-on: ubuntu-latest
    needs: build
    permissions:
      pages: write
      id-token: write

    steps:
      - uses: actions/deploy-pages@v2
